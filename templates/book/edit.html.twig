{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
{{ form_start(form) }}
    <div class="form-group">
      {{ form_row(form.title) }}
    </div>
    <div class="form-group">
      {{ form_row(form.author) }}
    </div>
    <div class="form-group pull-right">
      {{ form_row(form.save) }}
    </div>
 {{ form_end(form) }}
</div>
{% endblock %}
